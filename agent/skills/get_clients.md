---
description: Use when the user wants to see, list, or retrieve their active clients.
---

Use the `get_clients` tool when the user wants to view a list of their active clients.

When the results come back, present them to the user in a clear, readable format showing each client's name, contact details, preferences, and any other relevant information.

This tool does not require any input from the user - it will automatically find the current agent's active clients.
